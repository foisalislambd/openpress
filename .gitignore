node_modules/
dist/
.next/
.turbo/
*.tsbuildinfo
.env
*.log
uploads/
.pgdata/

# Uploaded themes & plugins (install via admin panel, not stored in git)
apps/api/plugins/
apps/api/themes-store/

# Built-in themes: only default is in the repo; others are uploaded or local
packages/themes/*
!packages/themes/default/
